import UIKit

extension ChatView {
    enum Constants {
        static let callBubbleEdgeInset: CGFloat = 10
        static let callBubbleSize = CGSize(width: 60, height: 60)
        static let chatTableViewInsets = UIEdgeInsets(top: 10, left: 0, bottom: 10, right: 0)
        static let topBannerViewAnimationDuration = 0.5
    }
}
